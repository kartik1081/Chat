import 'package:flutter/material.dart';

class NetError extends StatelessWidget {
  const NetError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
